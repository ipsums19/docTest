Per pujar la versio a PyPi executar:

a Pypi Test
#python setup.py register -r pypitest
#python setup.py sdist upload -r pypitest

a PyPi Live
#python setup.py register -r pypi
#python setup.py sdist upload -r pypi
