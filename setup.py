
from distutils.core import setup
setup(
  name = 'doctestfdbk',
  packages = ['doctestfdbk'], # this must be the same as the name above
  version = '0.1',
  description = 'A random test lib',
  author = 'Joan Barba',
  author_email = 'joan.barba@est.fib.upc.edu',
  url = 'http://jocs.lsi.upc.edu:5000/doctest-feedback', # use the URL to the mercurial repo
  download_url = 'crear un tag i insertar url del tag',
  keywords = ['testing', 'logging', 'example'], # arbitrary keywords
  classifiers = [],
)
